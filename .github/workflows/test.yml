name: Enterprise Deployment Pipeline

on:
  push:
    branches:
      - release/development
      - release/production
  workflow_dispatch:

# ✅ ให้สิทธิ์ GitHub Actions อ่านโค้ดและบันทึกผล Code Scanning
permissions:
  contents: read
  security-events: write

###############################################
# 🏗️ Job 1: Build .NET Core Application
###############################################
jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - name: 🛠 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔨 Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0'

      - name: 📦 Restore Dependencies
        run: dotnet restore

      - name: 🏗 Build and Publish
        run: dotnet publish -c Release -o out

      - name: 📁 Archive Build Output
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-build
          path: out/

###############################################
# 🔍 Job 2: Code Scanning (Security Scan / Static Analysis)
###############################################
  code-scanning:
    runs-on: ubuntu-latest
    needs: build-app
    steps:
      - name: 🛠 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔨 Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0'

      - name: 📦 Restore Dependencies
        run: dotnet restore

      - name: 🏗 Build Project (จำเป็นสำหรับ CodeQL)
        run: dotnet build --configuration Release

      - name: 🔍 Debug List all files
        run: ls -R

      - name: 🕵️ Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp
          build-mode: manual
          queries: security-extended,security-and-quality
          
      - name: 🚀 Perform Code Scanning
        uses: github/codeql-action/analyze@v3

###############################################
# 🐳 Job 3: Build Docker Image
###############################################
  build-docker-image:
    runs-on: ubuntu-latest
    needs: code-scanning
    steps:
      - name: 🛠 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔄 Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: 🏗 Build Docker Image
        run: docker build -t myapp:${{ github.sha }} .

      - name: 📦 Push Docker Image
        run: docker tag myapp:${{ github.sha }} mydockerhub/myapp:${{ github.sha }} && docker push mydockerhub/myapp:${{ github.sha }}


###############################################
# 🚀 Job 4: Post Docker Build (สำหรับ Development)
###############################################
  post-docker-build-dev:
    runs-on: ubuntu-latest
    needs: build-docker-image  # ✅ แยกออกมาจาก Production
    if: github.ref == 'refs/heads/release/development'
    steps:
      - name: ✅ Docker Build Completed for Development
        run: echo "Docker build completed for Development. Proceeding to deployment..."

###############################################
# 🚀 Job 5: Post Docker Build (สำหรับ Production)
###############################################
  post-docker-build-prod:
    runs-on: ubuntu-latest
    needs: build-docker-image  # ✅ แยกออกมาจาก Development
    if: github.ref == 'refs/heads/release/production'
    steps:
      - name: ✅ Docker Build Completed for Production
        run: echo "Docker build completed for Production. Proceeding to deployment..."

###############################################
# 🚀 Job 6: Deploy to Development Server
###############################################
  deploy-development-server:
    runs-on: ubuntu-latest
    needs: post-docker-build-dev  # ✅ เชื่อมกับ Dev เท่านั้น
    if: github.ref == 'refs/heads/release/development'
    steps:
      - name: 🚀 Deploying to Development Server
        run: echo "Deploying Docker Image to Development Server..."

###############################################
# 🚀 Job 7: Deploy to Production Server
###############################################
  deploy-production-server:
    runs-on: ubuntu-latest
    needs: post-docker-build-prod  # ✅ เชื่อมกับ Prod เท่านั้น
    if: github.ref == 'refs/heads/release/production'
    steps:
      - name: 🚀 Deploying to Production Server
        run: echo "Deploying Docker Image to Production Server..."

